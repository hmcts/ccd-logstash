input {
    jdbc {
			# TODO
    }
}

filter{
	# TODO
}

output {
    elasticsearch {
			hosts => [${ES_DATA_NODES_URL}]
			sniffing => false
			index => "%{[index_id]}"
			document_type => "_doc"
			document_id => "%{id}"
    }
}
