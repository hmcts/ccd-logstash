input {
    jdbc {
			# TODO
    }
}

filter{
	# TODO
}

#for performance reasons, ES_DATA_NODES_URL has to be a comma separated list of the cluster data nodes
output {
    elasticsearch {
			hosts => [ES_DATA_NODES_URL]
			sniffing => false
			index => "%{[index_id]}"
			document_type => "_doc"
			document_id => "%{id}"
    }
}
