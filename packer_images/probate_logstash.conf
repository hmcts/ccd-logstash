input {
    jdbc {
			# TODO
    }
}

filter{
	# TODO
}

#FIXME document_type is deprecated
output {
    elasticsearch {
			hosts => ["10.112.0.4:9200"]
			sniffing => false
			index => "%{[index_id]}"
			document_type => "case"
			document_id => "%{id}"
    }
}
